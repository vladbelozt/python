#задание 1
bytes_value = int(input("Введите количество байт: "))

bits_value = bytes_value * 8

print(f"{bytes_value} байт = {bits_value} бит.")


#5задание 2
print('Электронная книга по программированию на английском языке состоит из S страниц.\n'
      'В среднем на странице C строк по N символов.\n'
      'Используется кодировка UTF-8. Определите объём текстового файла в Кбайтах.')

s = int(input('Введите количество страниц: '))
c = int(input('Введите количество строк на странице: '))
n = int(input('Введите количество символов на строке: '))

print(f'Электронная книга по программированию на английском языке состоит из {s} страниц.\n'
      f'В среднем на странице {c} строк по {n} символов.\n'
      'Используется кодировка UTF-8. Определите объём текстового файла в Кбайтах.')

total_symbols = s * c * n
kb_size = total_symbols / 1024

print(f'{s} * {c} * {n} = {total_symbols} - Всего символов в книге.')
print(f'{total_symbols} / 1024 = {kb_size} - Кбайт.')
print(f'Ответ: {kb_size}.')
